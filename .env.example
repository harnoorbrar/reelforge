# Copy to .env and fill to UPGRADE quality. All are optional — ReelForge runs
# fully free (no keys) out of the box.
#
# OPENAI_API_KEY=sk-...        # enables richer LLM-written scripts
# FAL_KEY=                     # routes images through FAL FLUX (higher fidelity)
# REEL_VOICE=en-US-AriaNeural  # any edge-tts voice id
